const today = new Date();
today.getMonth();

const person = {
  age: 20
};
person.age;

class Color {
  rgb: string;
  orientation: number;
}
const red = new Color();
red.rgb = '#010101';
red.orientation = 1;
